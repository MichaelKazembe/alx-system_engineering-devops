#!/usr/bin/env bash
# install nginx web server
# adds 'Hello World!' to index page of web server

sudo apt-get -y install nginx
echo "Hello, World" | sudo tee /var/www/html/index.nginx-debian.html

sudo service nginx start
